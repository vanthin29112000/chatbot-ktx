services:
  - type: web
    name: chatbot-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 5000
      - key: FLASK_ENV
        value: production

